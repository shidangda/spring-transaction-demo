<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="email" column="email" />
        <result property="age" column="age" />
        <result property="createdTime" column="created_time" />
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, email, age, created_time, updated_time
    </sql>

    <insert id="insert" parameterType="com.example.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, age, created_time, updated_time)
        VALUES (#{username}, #{email}, #{age}, NOW(), NOW())
    </insert>

    <update id="updateUsernameById">
        UPDATE users SET username = #{username}, updated_time = NOW() WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/> FROM users ORDER BY id DESC
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/> FROM users WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/> FROM users WHERE email = #{email}
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(1) FROM users WHERE username = #{username}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(1) FROM users WHERE email = #{email}
    </select>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>


